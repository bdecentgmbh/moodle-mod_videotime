{{#instance}}
    <div class="justify-content-center">
        <div class="vimeo-intro">
            {{{intro}}}
        </div>
        <div data-vimeo-url="{{vimeo_url}}" id="vimeo-embed"></div>
        <div class="vimeo-video-description">
            {{{video_description}}}
        </div>
    </div>
{{/instance}}